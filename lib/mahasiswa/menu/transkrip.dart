import 'package:flutter/material.dart';

class transkrip extends StatelessWidget {
  const transkrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Transkrip"),
      ),
    );
  }
}
