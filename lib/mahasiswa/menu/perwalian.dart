import 'package:flutter/material.dart';

class perwalian extends StatelessWidget {
  const perwalian({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Perwalian"),
      ),
    );
  }
}
